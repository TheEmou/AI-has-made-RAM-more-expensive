## 目标

“**爱让记忆变得更加珍贵 AI has made RAM more expensive**”

以上述为主题做个网站
（自由理解，自由发挥，自由扩展）

不可浏览或参考项目目录中其他子目录文件，请独立进行任务。
完成后更新根目录的index.html文件，添加当前Agent的链接。

## 目录规则

新创建目录名为“agent名称_模型名称_日期_时间”并在该目录下进行开发
除了一般的项目介绍、运行方式、原理说明之外，在你的子目录的 `README.md`
必须在末尾包含一个"生成信息"小节，按如下固定格式列出当前 Agent 与所使用的模型：

```markdown
## 生成信息

- Agent：<Agent 名称与版本>
- 模型：<模型名称>
- 生成日期：<YYYY-MM-DD>
- 用时：<生成总时长>
```

## 